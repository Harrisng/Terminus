<Page x:Class="Terminus.UI.Pages.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      Background="{DynamicResource BgPrimary}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Calendar Area -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="24">
                <!-- Header with View Toggle -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="PageTitleText" Text="月曆" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock x:Name="PageSubtitleText" Text="點擊日期查看課程詳情" Style="{StaticResource PageSubtitleStyle}"/>
                    </StackPanel>

                    <!-- View Toggle + Month Title -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="{DynamicResource BgCard}" CornerRadius="8" Padding="2" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="MonthViewButton" Content="月" Width="36" Height="28" FontSize="13" FontWeight="SemiBold" Click="MonthViewButton_Click" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="Transparent" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button x:Name="WeekViewButton" Content="週" Width="36" Height="28" FontSize="13" FontWeight="SemiBold" Click="WeekViewButton_Click" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="Transparent" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </Border>
                        <TextBlock x:Name="MonthTitleText" Text="2026年9月" FontSize="{StaticResource FontSizeXL}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Navigation -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="PrevButton" Content="‹" Style="{StaticResource IconButtonStyle}" Click="PrevButton_Click"/>
                        <Button x:Name="TodayButton" Content="今天" Height="36" Padding="12,0" FontSize="13" Cursor="Hand" Click="TodayButton_Click" Margin="8,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" Background="{DynamicResource BgCard}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="NextButton" Content="›" Style="{StaticResource IconButtonStyle}" Click="NextButton_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Month View -->
                <Border x:Name="MonthViewBorder" Style="{StaticResource CardStyle}" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Day Headers -->
                        <Grid x:Name="DayHeadersGrid" Grid.Row="0" Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        </Grid>

                        <!-- Calendar Days Grid -->
                        <Grid x:Name="CalendarGrid" Grid.Row="1"/>
                    </Grid>
                </Border>

                <!-- Week View -->
                <Border x:Name="WeekViewBorder" Style="{StaticResource CardStyle}" Padding="0" Visibility="Collapsed">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="36"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Week Day Headers -->
                        <Grid x:Name="WeekHeadersGrid" Grid.Row="0" Background="{DynamicResource BgHeader}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        </Grid>

                        <!-- Week Time Grid -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <Grid x:Name="WeekTimeGrid" MinHeight="500"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Event Detail Panel -->
        <Border x:Name="DetailPanel" Grid.Row="1" Visibility="Collapsed" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderPrimary}" BorderThickness="0,1,0,0" Padding="24,16" MaxHeight="260">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="DetailDateText" Grid.Row="0" Grid.Column="0" Text="" FontSize="{StaticResource FontSizeLG}" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                <Button x:Name="CloseDetailButton" Grid.Row="0" Grid.Column="1" Content="✕" Style="{StaticResource IconButtonStyle}" Click="CloseDetailButton_Click" VerticalAlignment="Top"/>
                <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto" MaxHeight="200" Margin="0,8,0,0">
                    <StackPanel x:Name="EventListPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
