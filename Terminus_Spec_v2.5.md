# Terminus — v2.5 定稿

> **v2.5 相對 v2.4 的變更（專案升級）**
> 1. 專案正式命名為 **Terminus**（取「絕對終點」之意，原暫名：自動關機守門員）。
> 2. 封裝標準升級為 **MSI 安裝檔**，強制建立開始功能表捷徑以支援 **Windows Search** 搜尋。
> 3. 新增 **AUMID (AppUserModelID) 註冊** 約束（Z6），確保 Windows 10/11 原生通知綁定與搜尋索引運作正常。任務 T1、T15 估時上調，總計約 **60 小時**。

---

## ① 規格（Spec）

### 目標
一個 Windows 常駐小工具，每晚依你的 iCloud 課表算出「該收工的時間」，持續催你、必要時強制關機，防止你在隔天要上課的晚上通宵；沒課的日子也要有底線。

### 使用者與場景
- 使用者：只有你（Harris），一台 Windows，香港時間。
- **A｜隔天 09:30 有 ITP4416** → 今晚 00:15 開始催；不理它會自動延後，90 分鐘額度用完 → 最晚 01:45 關機。
- **B｜隔天上午沒課**（週末／公眾假期／課表無課／只有下午課）→ 00:30 開始催，按鈕可無限延（可越過 03:00）；不理它就一路自動延到 03:00 關機。
- **C｜你在電腦前睡著** → 沒人回應照樣延後到上限後關機，電腦不會亮到早上。
- **D｜你有真的重要事** → 被關機後自己重開機，**當晚工具完全安靜**，代價記在隔天的統計裡。
- **E｜你要用 AI 幫你通宵工作** → 開 AI 模式，電腦不關不睡、你去睡，任務跑完自動關機並推播手機。

### 功能範圍

**要做**
自動抓取 iCloud 課表與香港公眾假期；判定早課日／非早課日；就近寢週期計算催與硬關時間；通知＋三顆按鈕；沉默自動延後與額度；關機（統一收口、含阻擋重試與取消感知）；AI 通宵模式（含完成偵測與防睡眠）；完成推播（Webhook）；托盤常駐與自啟動；全圖形化設定；唯讀月曆頁；紀錄與統計；快取與離線；**標準化 MSI 安裝（含開始功能表捷徑與 Windows Search 整合）**；錯過處理、開機補跑、電源條件、通知強度、音訊豁免。

**明確不做**
密碼鎖或不可解除的機制；**任何阻止你自己重開機的機制**；跨平台；多人／給他人使用；在 app 內編輯 iCloud 日曆；帳號或雲端同步；另開一個乾淨日曆（你選擇沿用現有連結，並接受該連結對持有者公開你的行程）。

> **最高原則**：工具不做警察。它負責提醒、關機、記錄；要不要繞過，是你自己的事，代價只出現在統計數字裡。

### 執行緒原則（適用全篇）
> **UI 執行緒只負責顯示狀態，不得執行任何 I/O。** 抓取課表、Webhook、測試按鈕、關機呼叫、電源要求釋放、倒數計時，全部在背景執行緒執行。UI 凍結（Not Responding）一律視為缺陷。

### 資料取得（全部由程式自動連網，你不需下載／匯入／訂閱任何東西）
- **課表**：你現有的 iCloud 公開課表連結（`webcal://` → 以 https 抓取）。課表長期累積在同一日曆，換學期不需更換網址（網址欄位仍保留可改）。
- **公眾假期**：`https://www.1823.gov.hk/common/ical/tc.ics`（已驗證 HTTP 200，涵蓋 2025–2027，整天事件；可切換 `sc.ics`／`en.ics`）。
- 每 6 小時自動重抓 ＋ 手動刷新。

**抓取失敗的處理**
1. **有快取** → 用快取，畫面明示「這是 X 小時前的舊資料」。
2. **無快取且無網路** → **一律視為「非早課日」（寬鬆模式）**，並在通知與托盤**明寫「目前用的是預設值（抓不到課表）」**。
3. **快取檔損毀** → **不清空**，改名保留舊檔以便診斷，依第 2 條處理。
4. 設定可切換為**保守模式（比照早課日）**，預設寬鬆。
5. 連續多日抓取失敗 → 顯示警告。

### 判定規則
- **符合條件的事件**＝該日（香港時間）存在事件，且 `SUMMARY` 匹配 `^[A-Z]{2,4}-?\d{4}`、**非** `VALUE=DATE` 整天事件、`SUMMARY` 不含 `NO CLASS`。
- **「第一堂課」**＝該日符合條件事件中 `DTSTART` 最早者。全部換算香港時間（日曆混有 `Asia/Shanghai`、`Asia/Taipei`），並完整展開 `RRULE`／`EXDATE`／`RECURRENCE-ID`。
- **早課日／非早課日**：以**目標日（就寢週期所指向的那一天）00:00–12:00 之間有沒有符合條件的事件**判定。
  - **有** → **早課日**，套公式規則。
  - **沒有** → **非早課日**。涵蓋：完全沒課、只有下午課、週末、公眾假期、抓不到課表時。
  - 邊界：一堂課**剛好 12:00 開始** → 非早課日。
- 課表為準；公眾假期只用於月曆標示與說明，不覆蓋課表。

### 時間規則
| | 早課日 | 非早課日 |
|---|---|---|
| 開始催 | clamp(第一堂課 − 9h15m, 21:30, 03:00) | 00:30 |
| 手動延後 | 每次 30 分，總額度 **90 分** | 每次 30 分，**不限次數，可越過 03:00** |
| 沉默 | 自動延後、扣額度，扣完即關 | 自動延後，**最多自動到 03:00** 後關機 |
| 硬關 | min(催 + 90 分, 03:00) | 03:00（僅對沉默成立；主動按鈕可繼續） |

**9h15m ＝ 睡眠 6:00 ＋ 梳洗 0:30 ＋ 早餐 0:45 ＋ 通勤 1:45 ＋ 睡前 0:15**（五個獨立可改數字）。

**通用保險：最小預警時間 30 分鐘**
> 若算出的「催」與「硬關」相距不足 30 分鐘，強制把催往前推至「硬關 − 30 分」。

實例：09:00 課→23:45 催、01:15 關｜09:30 課→00:15 催、01:45 關｜10:30 課→01:15 催、02:45 關｜只有 13:30 的課→00:30 催、03:00 關。

### 就寢週期（Z2）
- **就寢週期 S**＝從某日 **12:00** 到隔日 **12:00**，以**週期 ID**（例如 `2026-06-17T12:00`）識別。
- **S 的目標課表日 ＝ 隔日**；S 內所有預告、催促、延後、額度、硬關都屬於 S；**額度在 S 開始（12:00）時重置**。
- **進行中的週期不因跨過 12:00 而中斷**。
- 週期比對**必須以 ID 為準**；**明文禁止 `has_rebooted = true` 這類布林旗標**。

### 通知與動作
- **催前 30 分鐘**跳一次預告（若最小預警保險把催往前推，預告也跟著往前）。
- **到催時間**跳主通知，三顆按鈕：**「再 30 分鐘」「現在就關」「AI 通宵模式」**。
- **沉默＝自動延後**。

### 關機流程：統一收口 + 取消感知（v2.4）

> **所有關機路徑**（額度用完、到硬關時刻、AI 完成、AI 超時、你按「現在就關」）**一律呼叫同一個 `ShutdownRequest()` 常式**，不得各自實作。
>
> `ShutdownRequest()` 內含：
> 1. 若處於 AI 模式 → **先釋放所有電源要求**：清除 `ES_CONTINUOUS`、撤銷 `PowerRequestSystemRequired`、還原待命逾時。
> 2. 在**隱藏訊息視窗**中註冊 `WM_QUERYENDSESSION`（一律回 `TRUE`，不阻止系統）與 `WM_ENDSESSION`。
> 3. 呼叫系統關機（**不得**用 `shutdown /t` 讓系統提前接管；倒數由程式自己算）。
> 4. **結果判定**：
>    - 收到 `WM_ENDSESSION` 且 wParam 為 **FALSE** → **關機被取消**（你在 Windows 的阻擋畫面按了「取消」，或系統中止）→ 記錄一筆「**使用者取消了關機**」→ 進入 **5 分鐘重試倒數**。
>    - 關機呼叫回傳錯誤碼 → 同樣進入 5 分鐘重試倒數。
>    - 重試 **3 次**後改用強制關機（`/f`）。
> 5. **`try/finally` 保證**：即使失敗、取消或例外，電源要求與待命逾時都必須還原。
> 6. 全程在背景執行緒執行。

### 重開機處理
> 程式啟動時讀取紀錄，比對**週期 ID**：若**同一個就寢週期 S 內**本程式曾執行過關機，則該 S 進入「停用」——
> ① **完全停止打擾**（不關機、不通知），只記一筆「本週期重開機（時間、距上次關機多久）」。
> ② **「連續達標天數」歸零**。
> ③ 月曆該日標示，統計頁顯示「本月重開過幾次」。
> ④ 選填「重開後跳一次提醒」，**預設關閉**。
> 跨到新 S → 恢復正常判定。

### 阻擋豁免規則（Z4）
**絕不因以下原因延後或取消關機**：偵測到音訊播放、全螢幕有影片、滑鼠鍵盤長時間無輸入、使用者不在場。
唯一允許阻擋的是**作業系統自己的「有程式未存檔」提示** → 走重試後強制關機，並依上節記錄「使用者取消」。
（已知案例：Chrome 的「關閉後繼續執行背景應用程式」會導致 Windows 阻擋關機。）

### AI 通宵模式

**觸發**
- 托盤／通知上的**手動按鈕**（一定在）。
- 選填「偵測到指定程式時**只提議**」：用同一套 Windows 通知附一顆按鈕；**不彈獨立視窗、不擋畫面、絕不自動開啟、當晚只提議一次**。
- 指定程式清單在設定視窗維護（Claude Code、DSH、Trae、ChatGPT agent…）。

**行為**
1. **不關機**（解除當晚所有關機排程，包括早課日的 03:00）
2. **防睡眠（三層保險，Z3 v2.4）**
   - ① `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)`（必做）
   - ② `PowerSetRequest(PowerRequestSystemRequired)`（必做）
   - ③ **待命逾時調整（改為「自動」模式）**：
     - **預設只在偵測到系統支援 Modern Standby 時才啟用**；否則完全不必動 `powercfg`。
     - **啟用前必須先寫前記錄**：把 `standby-timeout-ac` 原值寫入磁碟並標記「待還原」，之後才修改。
     - **只改 AC 值，DC 不動。**
     - **程式啟動時檢查該旗標**：若為「待還原」→ 背景優先執行還原。
     - **不覆蓋你的手改**：還原前比對現值，若已被你自己改動，只清旗標、不動設定。
     - **UI 提供「立即還原電源設定」按鈕。**
     - 結尾一律 `try/finally` 還原（正常結束路徑）。
   - `ES_AWAYMODE_REQUIRED` 為選填加項，**預設關**。
3. **關閉螢幕**
4. **每 30 分鐘跳一次「去睡吧，任務在跑」**（可關）
5. **完成偵測（四訊號）**
   - **訊號一**：設定清單中的 agent 程式**含其整棵子進程樹**全部退出
   - **訊號二：整機靜止偵測**
     - 取樣間隔 **30 秒**，判定窗口 **15 分鐘**（30 個樣本）
     - 四個訊號**同時成立**才算「靜止樣本」：**CPU**、**磁碟 < 1 MB/s**、**網路 < 200 KB/s**、**GPU < 5%**
     - **CPU 門檻（v2.4 修正）** ＝ **max( A, B )**
       - **A ＝ max(1%, 基線 × 1.5)**（基線＝啟動後前 5 分鐘的滾動中位數，抗背景雜訊）
       - **B ＝ 50% ÷ 邏輯核心數**（保證「半個邏輯核心滿載」必被判為有活動）
       - 驗算：4 核 12.5%／8 核 6.25%／16 核 3.125%／32 核 1.56%／64 核 1%（下限托住）
       - 若 **A > 2×B** → UI 警告「背景活動過高，完成偵測可能不準」
       - 保留手動覆寫輸入框
     - **容忍尖峰**：窗口內允許 **10%（3 個樣本）** 超標
     - 無可用 GPU 時**靜默略過 GPU 訊號**
   - **訊號三**：滿 N 小時強制關機（**1–12 小時，預設 4**，可關閉）
6. **完成後的處置**
   > 訊號一或訊號二成立時，檢查「**最近 5 分鐘內有沒有使用者輸入活動**（鍵盤、滑鼠、前景視窗切換）」：
   > - **沒有**（你睡著了）→ 直接 `ShutdownRequest()`。
   > - **有**（你醒著）→ **不關機**，跳一次「**AI 任務已完成**」通知，**切回正常狀態機**，恢復你的就寢倒數與剩餘額度。
   > - 之後又靜止超過 15 分鐘 → 靜止偵測再次觸發，這次直接關機。
   > （訊號三「超時保險」不受此判別影響，一律直接關機。）
7. **不設隔天起床提醒**
8. **紀錄**：月曆該日標「AI 通宵」，統計可見次數
9. **有課日亦可使用**

**明文限制（Z3d）**：`SetThreadExecutionState` **無法阻止你主動讓電腦睡眠**。**合上筆電蓋、按電源鍵仍會中斷任務**。AI 模式啟用時若偵測到系統即將睡眠，跳一次警告，並提示把「蓋上蓋子時」設為「不動作」。

### 完成推播 Webhook（Z5）

**服務類型（下拉明選，不得用 URL 猜測）**

| 類型 | Endpoint | Payload |
|---|---|---|
| **Discord** | 你提供的 Webhook URL | `{"content": "<訊息>"}`；上限 2000 字元，過長自動截斷 |
| **ntfy** | `https://ntfy.sh/<topic>`（可自填網域） | **純文字 body**，可選 `Title` 標頭 |
| **Telegram** | `https://api.telegram.org/bot<token>/sendMessage` | `{"chat_id": "...", "text": "<訊息>"}` |
| **自訂** | 任意 URL | 純 JSON 鍵值對，**可自訂 body 模板**（`{{shutdown_time}}`、`{{reason}}`） |

**預設不啟用。必備：設定裡一顆「發送測試訊息」按鈕，四種類型都要能驗證。**

**執行順序（不得變更，全程背景執行緒）**

*自動流程*
> 程式內部計時器到點 → 倒數 10 秒 → 發送 Webhook → 等回應或 **5 秒逾時** → 不論成功失敗，才呼叫 `ShutdownRequest()`。**只嘗試一次、不重試。**

*手動流程（你按「現在就關」）*
> ① **立即**把 UI 切成「正在關機…」→ ② 背景執行緒發送 Webhook，**逾時 2 秒** → ③ 才呼叫 `ShutdownRequest()`。
> （不可改成「異步 fire-and-forget 直接關機」——process 會被殺掉，訊息永遠送不出去。）

**HTTP 實作約束（v2.4）**：`HttpClient` 必須**明確設定 `Timeout`**（5 秒／2 秒），並以 `CancellationTokenSource` 雙保險；**嚴禁依賴系統預設的 100 秒逾時**，否則會卡死關機流程。

**失敗處理**：寫入紀錄；程式啟動後若發現未送出的紀錄，提供**選填補送**（不阻塞啟動）。

**可勾選的訊息欄位（部分或全部）**：關機時間／關機原因／今晚類別／明天第一堂課／本晚延後次數與分鐘數／AI 模式細節／是否被強制關機／隔天是否重開機。
**預設勾選**：關機時間＋關機原因。**觸發時機**：關機前 10 秒（預設）／偵測到重開機／每日總結。
**隱私提醒**：第 4 項會把你明天的課名送到第三方服務。

### 自啟動與權限（Z1 v2.4：預設改為登錄檔）

| 方案 | 何時使用 | 代價 |
|---|---|---|
| **`HKCU\Software\Microsoft\Windows\CurrentVersion\Run`**（**預設**） | 一律優先使用 | **零 UAC**；UI 隨時切換開關都不彈窗 |
| **工作排程器「登入時執行」**（進階選項） | 只在你實測「關機被拒」而必須提權時才啟用 | 需一次 UAC（建立工作時），之後不再詢問 |

- 關機只需要「Shut down the system」使用者權限（工作站預設授予 Users），所以**一般權限足夠**。
- 寫入 Run 鍵時路徑必須加引號；UI 切換自啟開關＝增刪該機碼。
- **偵測自啟動是否被停用**（你在「工作管理員 → 啟動」關掉它）→ 在 UI 明確提示「自啟動已失效」。
- 若啟用排程器方案，觸發延遲 15 秒（等網路與桌面就緒）。

### Windows Search 與系統通知整合（Z6 v2.5）
- 為了讓 Windows Search 能秒搜到，且確保 Windows 10/11 原生通知（Toast Notifications）運作正常，程式必須擁有明確的 **`AppUserModelID` (AUMID)**（例如 `Harris.Terminus.App`）。
- 安裝程式（MSI）必須在 **開始功能表（Start Menu）** 建立帶有 Icon 與正確 AUMID 的捷徑（Shortcut `.lnk`）。
- `.exe` 執行檔本身的 MetaData（Product Name, Description, Company）必須填寫完整，不可留空。

### 數據與狀態
- **本機保存**：全部設定、課表與假期快取（含時間戳）、事件紀錄（催時間／實際關機時間／延後次數／AI 模式與完成原因／是否重開機與間隔／是否被強制關／**使用者取消關機次數**／所屬就寢週期 ID）、單日例外標記、未送出的 Webhook 紀錄、連續達標天數、**電源設定待還原紀錄**。
- **狀態機**：待機 → 預告 → 催促 → 延後中 → 關機 ／ AI 模式（→ 進程樹退出／靜止完成／超時關機）／ 本週期停用。
- **啟動韌性**：設定或快取損毀時不崩潰，用預設值並提示；啟動時檢查電源待還原旗標。

### 已知取捨與待確認
1. 假設：Windows 10/11、單一使用者、香港時間。
2. 假設：靜止偵測閾值與容忍比例如上述，全部可調；**CPU 門檻必須實機校準**。
3. 假設：AI 模式提議「當晚一次」、完成判別門檻「5 分鐘」。
4. 抓不到課表時走寬鬆模式，代價是最多比真實早課日規則多放你 1 小時 15 分。
5. 課表日曆累積 2024–2027 歷史資料，工具只往後看不清理。
6. **待實測**：Modern Standby 機器上 `ES_AWAYMODE_REQUIRED` 是否被採用；GPU 計數器是否可讀；你的帳號是否保有「Shut down the system」權限；`WM_ENDSESSION(FALSE)` 在你的機器上是否確實送達。

---

## ② 任務拆解（Tasks）

**階段一：資料層與就寢週期（先做、不做 UI）**
| # | 任務 | 估時 | 依賴 | done when |
|---|---|---|---|---|
| T2 | iCal 抓取與解析（**必須用成熟函式庫**） | 3.5h | — | 用你的真實 iCal 驗收通過，事件與最早 DTSTART 正確 |
| T3 | 課表與假期快取、離線回退、無快取 fallback | 2h | T2 | 斷網用快取並標明時間；無網無快取時判定為非早課日並明示；快取損毀保留舊檔不崩潰 |
| T20 | 就寢週期（12:00 換日、週期 ID）與額度綁定 | 1.5h | T2 | 跨午夜不重置額度；11:50 的延後延續到 11:55 仍屬同一週期 ID |
| T4 | 早課／非早課判定＋時間計算＋最小預警（純函式） | 2.5h | T2,T20 | 六種輸入都輸出規格值；催與硬關至少相距 30 分鐘 |

**階段二：核心行為**
| # | 任務 | 估時 | 依賴 | done when |
|---|---|---|---|---|
| T6 | 延後與額度狀態機 | 2.5h | T4 | 早課日額度扣完即關；非早課日主動按可越過 03:00、沉默則 03:00 關 |
| T7 | `ShutdownRequest()` 收口 ＋ **取消感知訊息視窗** ＋ 阻擋重試 ＋ 釋放電源要求 | 2.5h | T6 | 五種路徑同一常式；`WM_ENDSESSION(FALSE)` 能辨識取消並記一筆；重試 3 次後強制關；`try/finally` 保證還原 |
| T8 | 重開機偵測、週期 ID 比對、代價紀錄 | 1.5h | T7 | 同週期重開完全安靜；紀錄含週期 ID 與間隔；達標天數歸零 |
| T5 | 通知與三顆按鈕 | 2h | T6 | 各按鈕觸發正確行為；最小預警生效時預告同步前移 |

**階段三：AI 模式**
| # | 任務 | 估時 | 依賴 | done when |
|---|---|---|---|---|
| T16 | AI 模式核心（不關機、關螢幕、30 分提醒、紀錄、月曆標記、解除當晚關機） | 3h | T4,T7 | 開啟後整晚不關，任務不中斷 |
| T21 | 防睡眠三層 ＋ **電源設定寫前記錄與啟動還原** ＋ UI 一鍵還原 | 2.5h | T16 | 正常結束與被 taskkill 兩種情況都能還原；你手改過則不覆蓋；UI 按鈕有效 |
| T17 | 完成偵測（進程樹退出 ＋ 整機靜止含 GPU、**多核 CPU 門檻**）＋ 5 分鐘輸入活動判別 ＋ 可調超時 | 5h | T16 | 32／64 核單核滿載不誤判；無人使用→關機、有人使用→恢復倒數；背景過高時警告；無 GPU 機器正常運作 |
| T18 | AI 模式程式偵測與「只提議」通知 | 1.5h | T16 | 指定程式啟動時跳一次提議，按下才進入，當晚不重複 |
| T22 | Webhook 模組（四種服務 payload、欄位勾選、測試按鈕、雙路徑逾時、**明確 HttpClient.Timeout**） | 4h | T7 | 四種類型都測通；逾時 5／2 秒生效（非 100 秒）；UI 全程不凍結 |

**階段四：外殼與周邊**
| # | 任務 | 估時 | 依賴 | done when |
|---|---|---|---|---|
| T1 | 專案骨架＋托盤常駐＋**註冊 AUMID (Z6)** | 2.5h | — | 托盤圖示出現；具備明確 AppUserModelID 並與系統通知綁定 |
| T19 | 自啟動（**預設 `HKCU\...\Run`**；排程器為進階選項）＋ 停用偵測 | 2h | T1 | 切換自啟開關零 UAC；Run 鍵被「工作管理員」停用時 UI 有提示 |
| T9 | 托盤顯示今晚判斷＋手動刷新＋「今晚不管」＋不確定狀態明示 | 2h | T4 | 托盤可讀出今晚判斷；抓不到課表時明確顯示用的是預設值 |
| T10 | 設定視窗（全部參數＋**電源還原按鈕**） | 6h | T4 | 每項改完立即生效，不重啟、不碰任何檔案 |
| T11 | 月曆頁（唯讀、上下月、假期標示、單日不關、重開機標記） | 4h | T4 | 可切上下月、標示重開機日、右鍵單日設「今晚不關」 |
| T12 | 紀錄與統計（達標天數歸零、取消關機次數） | 2.5h | T7,T8 | 平均關機時間、延後次數、連續達標天數、本月重開次數、取消關機次數 |
| T13 | 電源條件／開機補跑／錯過處理／失敗通知 | 3h | T4,T7 | 未插電不啟用；23:00 才開機立刻催；抓取失敗顯示警告 |
| T14 | 設定匯出／匯入 | 1h | T10 | 匯出檔匯入後設定完全一致 |
| T15 | 打包成 **MSI 安裝檔（WiX 或同級工具）** | 3.5h | 全部 | 雙擊 `.msi` 順利安裝/解除；**開始功能表出現捷徑，按 Win 鍵搜 Terminus 能立即找到**；常駐與關機動作有效 |

**建議施工順序**
1. **T2 → T3 → T20 → T4**（明確告訴 AI「**不要碰 UI**」）→ 驗收測試 A、B、C、G。
2. **T6 → T7 → T8 → T5** → 驗收測試 D、E、K。
3. **T16 → T21 → T17 → T18 → T22** → 驗收測試 F、I、H。
4. 最後 **T1、T19、T9–T14、T11**，收尾 T15。

**總估時**：約 **60 小時**（不含實機校準與除錯）。

---

## ③ 測試清單（Tests）

**A. 課表與假期解析**
- 每週重複的課展開成正確的每一天。
- `EXDATE` 排除的那天不算上課日。
- 單次改期（`RECURRENCE-ID`）以改期後時間與標題為準。
- Apple 非標準欄位不會讓解析崩掉或漏資料。
- `Asia/Shanghai`／`Asia/Taipei` 事件換算香港時間後不錯位。
- 整天事件不被採計。
- 標題含 `NO CLASS` 不被採計。
- 訂位、錄音、健身室、Online check In 等不符格式事件不被採計。
- 同一天多堂課取最早的一堂。
- 抓到空課表 → 視為非早課日且不當機。
- 公眾假期 iCal 抓取成功，農曆節日名稱與日期正確。

**B. 早課／非早課判定**
- 隔天 09:30 有課 → 早課日。
- 隔天只有 13:30 的課 → 非早課日，00:30 催、03:00 關。
- 隔天課程剛好 12:00 開始 → 非早課日。
- 隔天課程 11:30 開始 → 早課日。
- 週六日、公眾假期且無課 → 非早課日。
- 老師臨時加課（上午課）→ 下次抓取後類別跟著改。

**C. 時間計算（純函式）**
- 09:00 課 → 23:45 催、01:15 關。
- 09:30 課 → 00:15 催、01:45 關。
- 10:30 課 → 01:15 催、02:45 關。
- 非早課日 → 00:30 催、03:00 關。
- 推算早於 21:30 → 被下限夾住。
- 最小預警保險：任何參數組合下催與硬關相距 ≥ 30 分鐘；上限改 02:30 時催自動前移到 01:00。

**D. 通知與延後**
- 催前 30 分鐘出現預告；最小預警生效時同步前移。
- 到催時間跳主通知，三顆按鈕都在。
- 完全不理 → 早課日自動延後並扣額度，90 分用完即關。
- 非早課日完全不碰電腦 → 03:00 準時關機。
- 非早課日 02:50 按「再 30 分鐘」→ 03:20 關，之後不再自動延後。
- 非早課日一路按到 04:00 → 仍在運作。
- 按「現在就關」→ 立即關機。

**E. 關機流程**
- 五種關機路徑都走同一常式（日誌可證）。
- 無阻擋時到點直接關機。
- 有未存檔文件 → 阻擋提示，5 分鐘後重試，第 3 次強制關機。
- **在 Windows 阻擋畫面按「取消」→ 收到 `WM_ENDSESSION(FALSE)` → 進入 5 分鐘重試倒數，並記錄一筆「使用者取消了關機」。**
- 關機指令回傳錯誤碼 → 同樣進入重試倒數。
- Chrome 背景執行導致阻擋 → 走重試並強制關。
- AI 模式下關機 → 關機前電源要求已釋放、待命逾時已還原。
- 關機失敗／被取消時，電源要求與逾時仍被還原（`try/finally` 生效）。

**F. AI 通宵模式**
- 開啟後整晚不關機，當晚 03:00 的關機被解除。
- 8 小時無輸入任務未中斷（防睡眠三層有效）。
- AI 模式結束後，待命逾時設定回到原值。
- **AI 模式進行中被工作管理員強制結束 → 下次啟動自動還原待命逾時。**
- **AI 模式期間你自己改了待命逾時 → 啟動還原時不覆蓋你的值，只清旗標。**
- **UI「立即還原電源設定」按鈕有效。**
- 合上筆電蓋 → 出現警告，且如實記錄任務被中斷。
- 螢幕關閉，但行程仍在執行。
- 每 30 分鐘跳「去睡吧」；關閉後不再跳。
- agent 進程樹完全退出 → 觸發完成處置。
- 主進程閒置但子進程仍在跑（如編譯中）→ 不誤判完成。
- 一次性任務跑完、agent 視窗仍開著 → 靜止偵測接手，15 分鐘後關機。
- 背景有 Windows Update／防毒／Search Indexer 尖峰 → 因容忍 10% 樣本仍能正確判定。
- **32 邏輯核心機器上，單執行緒 agent 滿載一核（3.1%）→ 不判定為靜止。**
- **64 邏輯核心機器上，單執行緒 agent 滿載一核（1.56%）→ 不判定為靜止。**
- **8 核機器上門檻為 6.25%，滿載一核（12.5%）→ 不判定為靜止。**
- **背景活動過高（A > 2×B）→ UI 顯示警告。**
- 本地 LLM 跑 GPU 推理中（CPU 低但 GPU 滿載）→ 不誤判完成。
- 無 GPU 的機器 → GPU 訊號靜默略過，其餘訊號照常運作。
- 修改任一閾值 → 行為隨設定改變。
- 完成時 5 分鐘內無任何輸入（睡著）→ 直接關機。
- 完成時 5 分鐘內有鍵鼠活動（醒著）→ 不關機，跳「AI 任務已完成」並恢復正常倒數、剩餘額度仍可用。
- 恢復正常倒數後又靜止 15 分鐘 → 第二次觸發，直接關機。
- 超時保險不受 5 分鐘判別影響 → 一律直接關機。
- 超時保險 1 小時與 12 小時各驗一次。
- 指定程式啟動時跳一次提議；當晚不重複；未按提議不會自動進入。
- 月曆標「AI 通宵」，統計可見次數。
- AI 模式中途關閉 → 回到正常流程。
- AI 模式進行中你手動關機 → 紀錄標明「使用者手動結束」，不再補關。

**G. 就寢週期**
- 週二 23:45 的預告與週三 00:15 的催屬同一週期 ID，額度不重置。
- 非早課日按到 11:50，11:55 仍在同一週期、額度不回復。
- 週期結束後（關機）12:00 才建立新週期 ID。
- 凌晨 02:00 開機 → 歸屬於「昨天 12:00 起的週期 ID」。
- 11:55 與 12:01 各重開一次 → 前者停用（同週期），後者不停用（新週期）。

**H. 音訊豁免**
- 背景 YouTube 自動連播中，到硬關時刻照樣關機。
- 全螢幕影片播放中，沉默超時照樣關機。
- 滑鼠鍵盤長時間無輸入 → 不構成延後理由。

**I. Webhook**
- 四種服務類型測試訊息都成功送出。
- Discord 訊息超過 2000 字元 → 自動截斷且不報錯。
- 自訂類型可使用 body 模板與變數。
- 勾「關機時間＋原因」→ 關機前 10 秒收到正確訊息。
- 勾選全部八項 → 訊息含所有欄位且內容正確。
- 自動流程：訊息送出後才呼叫關機（日誌時間戳可證）。
- 自動流程斷網 → 5 秒逾時、不重試、關機準時。
- 手動按「現在就關」→ UI 立即顯示「正在關機…」；最多等 2 秒；關機仍執行。
- **四種服務在斷網時，UI 全程不凍結。**
- **逾時確實為 5／2 秒，而非 HttpClient 預設的 100 秒。**
- 下次啟動時發現未送出紀錄 → 補送可用且不阻塞啟動。

**J. 自啟動與權限**
- **切換自啟開關全程零 UAC。**
- Run 鍵寫入的路徑有加引號且能正常启动。
- **在「工作管理員 → 啟動」停用本程式 → UI 顯示「自啟動已失效」。**
- 進階選項切到工作排程器後靜默常駐、無 UAC 視窗（建立時一次 UAC 為預期行為）。
- 一般權限下 `shutdown` 成功（若失敗才啟用提權方案並重測）。

**K. 重開機**
- 關機後 30 分鐘內手動重開 → 該週期完全安靜。
- 關機後 2 小時才重開（同一週期）→ 同樣安靜。
- 隔天 06:00 起床用電腦（同一週期）→ 安靜。
- 重開機後「連續達標天數」歸零。
- 紀錄含週期 ID 與距上次關機多久；月曆與統計可見。
- 選填的當晚提醒預設不跳；開啟後只跳一次。
- 跨過 12:00 後的週期 → 恢復正常判定。

**L. 抓取與離線**
- 有快取時斷網 → 用快取並標明時間。
- 首次啟動無網無快取 → 非早課日，且通知與托盤明示「用的是預設值」。
- 快取檔損毀 → 保留舊檔、不崩潰、走預設值流程。
- 設定切換為保守模式 → 無快取時改比照早課日。
- 連續多日失敗 → 顯示警告。
- 手動刷新立即重抓並更新（UI 不凍結）。
- 課表網址失效 → 明確報錯。
- 1823 改版或停止提供 → 明確錯誤，不連帶爛掉早課判定。

**M. 邊界與韌性**
- 催促期間電腦是關著的（你外出）→ 開機後只提醒、不補關。
- 23:00 才開機且已過催時間 → 立刻開始催。
- 系統時間被手動調整 → 不重複關機、不亂跳。
- 螢幕鎖定中 → 通知與關機照常。
- 未插電（筆電外出）→ 依設定不啟用。
- 延後途中你手動關機 → 不留殘狀態。
- 設定或快取損毀 → 用預設值啟動並提示，不崩潰。
- **斷電後重啟 → 待還原旗標生效，電源設定被還原。**

**N. 設定與持久化**
- 每個參數改完立即生效，不需重啟。
- 匯出再匯入，行為完全一致。
- 設定值不合理（睡眠 30 小時、超時保險 0 小時）→ 提示而非崩潰。

**O. 月曆頁**
- 可切換上／下個月。
- 公眾假期顯示正確節日名稱。
- 每格顯示的催／硬關時間與實際運算一致。
- 重開機日有標示。
- 設某天「今晚不關」→ 該天不觸發關機，隔天恢復。

**P. 安裝與整合 (Z6)**
- 執行 MSI 安裝檔無須手動設定路徑即可完成安裝。
- 安裝後，按 `Win` 鍵輸入 "Terminus" 應能立即在搜尋結果出現並帶有正確 Icon。
- 測試 Windows 10/11 系統通知（Toast）能正常彈出，不會被系統當成無名進程阻擋。
- 解除安裝能乾淨移除檔案與開始功能表捷徑。

**仍未測試／待確認**
- 靜止偵測門檻（尤其 CPU 公式）在真實 AI 任務下的準確率——**必須實機校準**。
- Modern Standby 機器上 `ES_AWAYMODE_REQUIRED` 是否被採用。
- GPU 計數器在你的機器上是否可讀。
- `WM_ENDSESSION(FALSE)` 在你的 Windows 版本上是否確實送達。
- Windows 11 通知在獨佔全螢幕遊戲中是否真的可見。
- 1823 若停止提供或改版，假期判定會失效。
- 旅行跨時區時，一律用香港時間是否仍合理。
- agent 進程樹偵測在 Trae（IDE，常駐不退）上的有效性——只能靠靜止偵測。

---

## 附錄Ａ｜可自訂項目（全圖形介面，零程式碼）

A 個人數字：睡眠、睡前緩衝、梳洗、早餐、通勤
B 時間規則：上限、下限、最小預警時間、週末上限是否不同、學期起訖日
C 事件判定：包含正則／關鍵字、排除關鍵字、是否採計整天事件、早課分界時刻（預設 12:00）
D 通知：提前量、預告次數、通知文字、音效
E 到點動作：關機／睡眠／休眠／鎖定、是否強制關掉擋著的程式
F 額度：總額度、每次延後長度、能否預支明天額度
G 例外：今晚跳過、停用日期範圍、考試週模式、假期清單
H 記錄：保留天數、平均關機時間、延後次數
I 可見性：托盤顯示、未來 7 天預覽、手動刷新
J 系統：自啟動方式（**Run 鍵預設／排程器進階**）、錯過關機時間的處理
K 介面：語言、深色模式
L 日曆：日曆網址、抓取頻率、抓不到課表時的行為（寬鬆／保守）
M 統計：日均關機、延後次數、連續達標天數、本月重開次數、取消關機次數
N 通知強度：只跳通知／音效／全螢幕
O 遊戲偵測：全螢幕時更強的通知（提前 15 分鐘）
P 電源條件：只在插電時啟用
Q 托盤一鍵：「今晚照課表關」／「今晚不管」
R 設定匯出／匯入
S 錯過處理：開機後只提醒不補關
T 手動假期
U 抓取失敗通知
V 判斷透明化
W 開機補跑
X 作業保護：指定程式在跑就延後 10 分鐘
Y 排除日期：一整段停用
Z 重開機：當晚提醒開關（**預設關**）
**電源**：待命逾時調整模式（自動／一律開／一律關）、**立即還原電源設定按鈕**
AI-n：agent 清單、靜止偵測參數（取樣／窗口／**CPU 門檻（含手動覆寫）**／磁碟／網路／GPU 開關與閾值／容忍比例／基線倍數）、完成判別輸入活動門檻（預設 5 分鐘）、超時保險（1–12 小時）、30 分鐘提醒、提議開關、away mode 開關
WH-n：Webhook 服務類型（下拉）、URL／token／topic、欄位勾選、觸發時機、測試按鈕、補送開關、自訂 body 模板

## 附錄Ｂ｜預設值一覽

| 參數 | 預設 |
|---|---|
| 睡眠／睡前／梳洗／早餐／通勤 | 6:00／0:15／0:30／0:45／1:45 |
| 課前緩衝合計 | 9 小時 15 分 |
| 早課分界 | 12:00 |
| 早課日催下限／上限 | 21:30／03:00 |
| 早課日額度／每次延後 | 90 分／30 分 |
| 最小預警時間 | 30 分 |
| 非早課日催／沉默自動延後終點 | 00:30／03:00 |
| 預告提前量 | 30 分 |
| 關機阻擋重試 | 每 5 分 ×3 後強制 |
| 抓取頻率 | 6 小時 |
| 抓不到課表時 | 非早課日（寬鬆） |
| 課程編號正則 | `^[A-Z]{2,4}-?\d{4}` |
| 排除關鍵字 | `NO CLASS` |
| 靜止偵測 | 取樣 30 秒／窗口 15 分／**CPU ＝ max(max(1%, 基線×1.5), 50%÷核心數)**／磁碟 1 MB/s／網路 200 KB/s／GPU 5%／容忍 10% |
| AI 完成判別輸入活動門檻 | 5 分鐘 |
| AI 超時保險 | 4 小時（1–12，可關閉） |
| AI 提醒間隔 | 30 分鐘 |
| AI 提議／away mode | 當晚一次／關 |
| 待命逾時調整 | **自動（僅 Modern Standby 啟用）** |
| 重開機當晚提醒 | 關 |
| 自啟動 | **HKCU\Software\Microsoft\Windows\CurrentVersion\Run** |
| Webhook | 不啟用；服務類型下拉；勾「關機時間＋原因」；關機前 10 秒；自動 5 秒／手動 2 秒逾時；不重試 |

## 附錄Ｃ｜已知限制（我們管不了的）

1. 合上筆電蓋或按電源鍵 → 任務會中斷，任何 API 都攔不住。
2. Windows 的「有程式未存檔」提示只能靠重試＋強制關繞過，有極小機率丟失未存檔內容。
3. 若電源計畫或公司政策鎖死待命設定，防睡眠可能失效。
4. 公開 iCloud 連結的有效性取決於你沒有取消分享；1823 服務取決於政府持續提供。
5. iCal 函式庫對 Apple 非標準欄位的支援程度不一，需以你的真實檔案驗收。
6. **重開機是你的權利**：工具不做警察，本設計不阻止、不追蹤位置、不加密碼。
7. 靜止偵測本質上是啟發式；CPU 門檻必須用你的機器實測校準。

## 附錄Ｄ｜交給實作 AI 的前言指令（可直接複製）

> 這是一份 **Terminus** (Windows 常駐防熬夜與 AI 任務管理工具，使用 C# WPF 或 Python PyQt/Tkinter) 的 v2.5 規格書。請嚴格遵守：
> 1. 業務邏輯以規格書為準，**未經確認不得修改**。
> 2. **遇到規格矛盾、資訊不足或技術上無法實作時，必須停下來提出，不得自行決定或繞道。**
> 3. 請先讀完全篇，然後**直接從「階段一：資料層與就寢週期（T2、T3、T20、T4）」開始實作，不要先寫 UI**。
> 4. 每完成一個 Task，必須寫出對應的 Unit Test 並通過（尤其測試清單 A、B、C），才能進入下一階段。
> 5. **UI 執行緒只負責顯示**；所有網路請求、關機呼叫、電源要求操作一律在背景執行緒。
> 6. 所有關機路徑一律呼叫同一個 `ShutdownRequest()` 常式，不得各自實作。
> 7. **Webhook 的 `HttpClient` 必須明確設定 `Timeout`（5 秒／2 秒）並搭配 `CancellationTokenSource`，嚴禁依賴預設的 100 秒逾時**，否則會卡死關機流程。
> 8. **打包 MSI 時，若使用 C# 請優先考慮使用 WiX Toolset 或 Microsoft Visual Studio Installer Projects；若使用 Python 請使用 `cx_Freeze` + `bdist_msi` 或先轉 exe 再以 WiX 打包。必須保證安裝後會自動建立 Start Menu 捷徑以支援 Windows Search，且註冊正確的 AppUserModelID (AUMID) 以支援系統通知。**
